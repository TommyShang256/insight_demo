# Cube Profiling：紧凑摘要设计

## 边界

SQL 决定粒度和指标计算，profile 描述返回结果。同维度过滤 OR、跨维度 AND，已经由原查询执行，不重复过滤。结果字段对应维度 name 和指标 result_column，aggregation 仅解释原查询语义。

两路不输出业务汇总，不依赖公式或辅助列。空值、重复、分布均针对查询结果，不能反推源明细质量。结果行占比的分母包含所有输入行，不是业务贡献比例。

## 实现层次

1. 原子描述统计保留于 statistic_profile.py、dimension_profile.py，详细入口显式命名为 *_detail。
2. compact_profile.py 构建默认全量分布摘要，列定义只出现一次，具体统计行按列式数组表达。
3. query_profile.py 从同一原结果筛选并分页返回完整组/时间桶/切片分布，无需模型先读取所有明细。

不新增服务或依赖。当前实现优先解决上下文大小，仍完整持有输入并计算全量组统计，不宣称已解决百万行流式执行。

## StatisticProfile 3.0

保留 scope、dataset、metric_schema、stats_columns、metric_stats、time。每个指标默认只输出有效数值、NULL、非数值、非有限值数量与 min/P05/P50/P95/max/stddev_pop；详细分布可按需查询。

时间先按原 hour/day/month 粒度识别实际桶和连续观测段。段数不超过窗口预算时，每段至少一窗，剩余预算按桶数分配；段数超过预算时连续打包多个段并标记 spans_unobserved_gap。默认最多 24 窗，所有有效时间行和桶都被表示，未出现时间不会补零。

每个窗口从原结果行直接计算分位数，不由分位数再汇总；极值附原数组零基索引与本地时间。窗口可以含多个普通维度系列，表示混合返回值分布，不能称为单一业务走势。空/非法时间计数保留，不纳入窗口。

## DimensionProfile 2.0

每个单维和已选择二阶组合提供：

- 全量频次概况：组数、非空组数、含空行数、组大小 min/P10/P50/P90/max、单例数量与 Top-10 频次覆盖。
- 全量组间分布：每个含数值组的指标中位数与总体标准差，再求这些组统计的 min/P10/P50/P90/max。每组等权，明确参与组数，不与结果行总体分布混同。
- 有限展开：默认最多 8 个代表组。低基数完整展示；高基数轮转选择高频以及各指标中位数/离散程度两端和中间组，去重后截止。保留样本量、选择原因和类型化键，不自动判断异常。
- 覆盖：全部组参与摘要与具体展开覆盖分别报告。频次 Top-10 覆盖不等于代表组覆盖；二阶未计算对显式列出。

组合选择仍按单维实际组数乘积和维度名，默认最多 6 对。NULL 与字符串、数字、布尔值保持类型区别。不生成 Other 的虚构组统计。

## 输出预算

每路默认最多 16000 个紧凑 UTF-8 JSON 字节，两路合计最多 32000（不含文件结尾换行）。不依赖粗略的 token/字符换算，不宣称精确模型 token 数。

时间超预算时减少展示窗并重分，保留全范围；维度超预算先减组合代表，再减单维代表，仍保留全部组间分布和频次概况。核心统计自身超限则报错，由调用方显式限定维度/指标或提高预算，不能静默截断输入口径。

## 明细契约

query_details 接受 kind=groups/time/slice、类型化维度筛选（AND）、指标列表、左闭右开时间范围、offset/limit。groups 可请求单维或组合；time 返回原桶；slice 描述筛选后的原行。明细不重算业务指标。

分组稳定按行数及类型化键排序。每页有总项数、返回数、has_more、next_offset 和绑定数据及 cube 的 SHA-256；消费方必须核对跨页哈希与查询。响应超字节预算会减小本页数量，单项仍超限则报错。增加筛选不改变结果行占比分母。

## 当前 mock 与校验

源查询产生 279 行结果；时间摘要以 24 窗覆盖全部 279 个小时桶。channel/region 完整展开；product_id 的全部 77 组参与频次和组间分布，默认展开 8 组。两路按统一紧凑编码由约 453 KB/102 KB 降为约 12 KB/16 KB。

旧 16 项测试继续验证原子统计与详细输出，新增 6 项验证默认紧凑结构、全量口径、压缩预算和明细分页。委派 skill 已更新为紧凑 StatisticProfile 3.0 / DimensionProfile 2.0，仍以实际结果行作为 subagent 的观察对象。
